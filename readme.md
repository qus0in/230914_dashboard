```bash
$ curl https://www.toptal.com/developers/gitignore/api/python > .gitignore
$ python3 -m venv venv
$ source venv/bin/activate
$ pip install streamlit finance-datareader
$ pip freeze > requirements.txt
```